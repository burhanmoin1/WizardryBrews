'use client';

import './HeroSection.css';

import { motion} from "framer-motion";

const HeroSection = () => {
    
    return (
        <div className="HeroSection">
            <h1 className='HeroHeading'>WizardryBrews</h1>
        </div>
    );
};

export default HeroSection;